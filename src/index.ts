export default async({ core }) => {core.notice(
		"Hello from the workflow-runtime-action! This action detects the JavaScript runtime and package manager used in your project, and sets outputs accordingly.",
	);
