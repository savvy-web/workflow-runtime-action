name: Test - Node.js Runtime

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "dist/**"
      - "action.yml"
      - ".github/workflows/test-node.yml"
  pull_request:
    paths:
      - "src/**"
      - "dist/**"
      - "action.yml"
      - ".github/workflows/test-node.yml"

jobs:
  test-npm:
    name: Node.js with NPM
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup isolated test environment
        run: |
          mkdir -p /tmp/test-workspace
          cp -r $GITHUB_WORKSPACE/repo/__fixtures__/node-pnpm/* /tmp/test-workspace/
          cp -r $GITHUB_WORKSPACE/repo/dist /tmp/test-workspace/
          cp $GITHUB_WORKSPACE/repo/action.yml /tmp/test-workspace/

      - name: Setup Node.js with NPM
        id: setup
        uses: ./
        with:
          package-manager: npm
          node-version: "20.x"

      - name: Verify installation
        run: |
          echo "## 📦 NPM Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Runtime: \`${{ steps.setup.outputs.runtime }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Node.js: \`${{ steps.setup.outputs.node-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Package Manager: \`${{ steps.setup.outputs.package-manager }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          node --version
          npm --version
          echo "✅ NPM setup successful" >> $GITHUB_STEP_SUMMARY

  test-pnpm:
    name: Node.js with PNPM
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup isolated test environment
        run: |
          mkdir -p /tmp/test-workspace
          cp -r $GITHUB_WORKSPACE/repo/__fixtures__/node-pnpm/* /tmp/test-workspace/
          cp -r $GITHUB_WORKSPACE/repo/dist /tmp/test-workspace/
          cp $GITHUB_WORKSPACE/repo/action.yml /tmp/test-workspace/

      - name: Setup Node.js with PNPM (auto-detect)
        id: setup
        uses: ./

      - name: Verify installation
        run: |
          echo "## 📦 PNPM Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Runtime: \`${{ steps.setup.outputs.runtime }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Node.js: \`${{ steps.setup.outputs.node-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Package Manager: \`${{ steps.setup.outputs.package-manager }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          node --version
          pnpm --version
          if [ "${{ steps.setup.outputs.package-manager }}" != "pnpm" ]; then
            echo "❌ Expected pnpm but got ${{ steps.setup.outputs.package-manager }}" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          echo "✅ PNPM auto-detection successful" >> $GITHUB_STEP_SUMMARY

  test-yarn:
    name: Node.js with Yarn
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup isolated test environment
        run: |
          mkdir -p /tmp/test-workspace
          cp -r $GITHUB_WORKSPACE/repo/__fixtures__/node-pnpm/* /tmp/test-workspace/
          cp -r $GITHUB_WORKSPACE/repo/dist /tmp/test-workspace/
          cp $GITHUB_WORKSPACE/repo/action.yml /tmp/test-workspace/

      - name: Setup Node.js with Yarn (auto-detect)
        id: setup
        uses: ./

      - name: Verify installation
        run: |
          echo "## 📦 Yarn Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Runtime: \`${{ steps.setup.outputs.runtime }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Node.js: \`${{ steps.setup.outputs.node-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Package Manager: \`${{ steps.setup.outputs.package-manager }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          node --version
          yarn --version
          if [ "${{ steps.setup.outputs.package-manager }}" != "yarn" ]; then
            echo "❌ Expected yarn but got ${{ steps.setup.outputs.package-manager }}" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          echo "✅ Yarn auto-detection successful" >> $GITHUB_STEP_SUMMARY

  test-version-file:
    name: Node.js Version from .nvmrc
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup isolated test environment
        run: |
          mkdir -p /tmp/test-workspace
          cp -r $GITHUB_WORKSPACE/repo/__fixtures__/node-pnpm/* /tmp/test-workspace/
          cp -r $GITHUB_WORKSPACE/repo/dist /tmp/test-workspace/
          cp $GITHUB_WORKSPACE/repo/action.yml /tmp/test-workspace/

      - name: Setup Node.js from .nvmrc
        id: setup
        uses: ./

      - name: Verify version file detection
        run: |
          echo "## 📄 Version File Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Version File: \`${{ steps.setup.outputs.node-version-file }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Version Source: \`${{ steps.setup.outputs.node-version-source }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Detected Version: \`${{ steps.setup.outputs.node-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.setup.outputs.node-version-file }}" != ".nvmrc" ]; then
            echo "❌ Expected .nvmrc detection" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
          echo "✅ .nvmrc file detected successfully" >> $GITHUB_STEP_SUMMARY

  test-lts-version:
    name: Node.js LTS Version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup isolated test environment
        run: |
          mkdir -p /tmp/test-workspace
          cp -r $GITHUB_WORKSPACE/repo/__fixtures__/node-pnpm/* /tmp/test-workspace/
          cp -r $GITHUB_WORKSPACE/repo/dist /tmp/test-workspace/
          cp $GITHUB_WORKSPACE/repo/action.yml /tmp/test-workspace/

      - name: Setup Node.js LTS
        id: setup
        uses: ./
        with:
          node-version: "lts/*"

      - name: Verify LTS installation
        run: |
          echo "## 🔖 LTS Version Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Installed Version: \`${{ steps.setup.outputs.node-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          node --version
          echo "✅ LTS version installed successfully" >> $GITHUB_STEP_SUMMARY

  summary:
    name: Node.js Tests Summary
    runs-on: ubuntu-latest
    needs: [test-npm, test-pnpm, test-yarn, test-version-file, test-lts-version]
    if: always()
    steps:
      - name: Generate summary
        run: |
          echo "## 🧪 Node.js Runtime Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| NPM | ${{ needs.test-npm.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| PNPM | ${{ needs.test-pnpm.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Yarn | ${{ needs.test-yarn.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Version File (.nvmrc) | ${{ needs.test-version-file.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| LTS Version | ${{ needs.test-lts-version.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Check if all tests passed
          if [ "${{ needs.test-npm.result }}" == "success" ] && \
             [ "${{ needs.test-pnpm.result }}" == "success" ] && \
             [ "${{ needs.test-yarn.result }}" == "success" ] && \
             [ "${{ needs.test-version-file.result }}" == "success" ] && \
             [ "${{ needs.test-lts-version.result }}" == "success" ]; then
            echo "### ✅ All Node.js tests passed!" >> $GITHUB_STEP_SUMMARY
            exit 0
          else
            echo "### ❌ Some tests failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
