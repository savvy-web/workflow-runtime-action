name: Test - Bun Runtime

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "dist/**"
      - "action.yml"
      - ".github/workflows/test-bun.yml"
  pull_request:
    paths:
      - "src/**"
      - "dist/**"
      - "action.yml"
      - ".github/workflows/test-bun.yml"

jobs:
  test-bun-auto-detect:
    name: Bun - Auto-detect from package.json
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test fixture
        uses: ./.github/actions/setup-fixture
        with:
          fixture: bun-minimal

      - name: Setup Bun (auto-detect)
        id: setup
        uses: ./

      - name: Verify Bun installation
        run: |
          echo "## 🚀 Bun Auto-detect Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Runtime: \`${{ steps.setup.outputs.runtime }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Bun Version: \`${{ steps.setup.outputs.bun-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Package Manager: \`${{ steps.setup.outputs.package-manager }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Verify Bun is installed
          bun --version

          # Verify package manager is bun
          if [ "${{ steps.setup.outputs.package-manager }}" != "bun" ]; then
            echo "❌ Expected bun package manager" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          # Verify runtime output
          if [ "${{ steps.setup.outputs.runtime }}" != "bun" ]; then
            echo "❌ Expected bun runtime" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          # Run TypeScript file
          bun run index.ts

          echo "✅ Bun auto-detection successful" >> $GITHUB_STEP_SUMMARY

  test-bun-explicit:
    name: Bun - Explicit version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test fixture
        uses: ./.github/actions/setup-fixture
        with:
          fixture: bun-minimal

      - name: Setup Bun (explicit version)
        id: setup
        uses: ./
        with:
          package-manager: bun
          bun-version: "1.1.42"

      - name: Verify Bun installation
        run: |
          echo "## 🎯 Bun Explicit Version Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Bun Version: \`${{ steps.setup.outputs.bun-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Verify version
          INSTALLED_VERSION=$(bun --version)
          echo "Installed: $INSTALLED_VERSION"

          if [ "${{ steps.setup.outputs.bun-version }}" != "1.1.42" ]; then
            echo "❌ Expected version 1.1.42" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          bun run test.js
          echo "✅ Explicit version installation successful" >> $GITHUB_STEP_SUMMARY

  test-bun-package-install:
    name: Bun - Package installation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test fixture
        uses: ./.github/actions/setup-fixture
        with:
          fixture: bun-deps

      - name: Setup Bun and install dependencies
        id: setup
        uses: ./
        with:
          install-deps: true

      - name: Verify dependencies
        run: |
          echo "## 📦 Bun Package Installation Test" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Check node_modules exists
          if [ ! -d "node_modules" ]; then
            echo "❌ node_modules not found" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          # Run test file
          bun run test.js

          echo "✅ Dependencies installed successfully" >> $GITHUB_STEP_SUMMARY

  test-bun-lockfile:
    name: Bun - Lock file handling
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test fixture
        uses: ./.github/actions/setup-fixture
        with:
          fixture: bun-lockfile

      - name: Setup Bun with lockfile
        id: setup
        uses: ./

      - name: Verify lockfile detection
        run: |
          echo "## 🔒 Bun Lockfile Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Verify bun.lockb exists
          if [ ! -f "bun.lockb" ]; then
            echo "❌ bun.lockb not found" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          echo "✅ Lockfile handled correctly" >> $GITHUB_STEP_SUMMARY

  summary:
    name: Bun Tests Summary
    runs-on: ubuntu-latest
    needs:
      [
        test-bun-auto-detect,
        test-bun-explicit,
        test-bun-package-install,
        test-bun-lockfile,
      ]
    if: always()
    steps:
      - name: Generate summary
        run: |
          echo "## 🧪 Bun Runtime Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Auto-detect | ${{ needs.test-bun-auto-detect.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Explicit Version | ${{ needs.test-bun-explicit.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Package Install | ${{ needs.test-bun-package-install.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Lockfile Handling | ${{ needs.test-bun-lockfile.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Check if all tests passed
          if [ "${{ needs.test-bun-auto-detect.result }}" == "success" ] && \
             [ "${{ needs.test-bun-explicit.result }}" == "success" ] && \
             [ "${{ needs.test-bun-package-install.result }}" == "success" ] && \
             [ "${{ needs.test-bun-lockfile.result }}" == "success" ]; then
            echo "### ✅ All Bun tests passed!" >> $GITHUB_STEP_SUMMARY
            exit 0
          else
            echo "### ❌ Some tests failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
