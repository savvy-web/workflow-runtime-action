name: Test - Features (Biome, Turbo, Cache)

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "src/**"
      - "dist/**"
      - "action.yml"
      - ".github/workflows/test-features.yml"
  pull_request:
    paths:
      - "src/**"
      - "dist/**"
      - "action.yml"
      - ".github/workflows/test-features.yml"

jobs:
  test-biome-auto-detect:
    name: Biome - Auto-detect from config
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test environment
        run: |
          # Remove repository config files
          rm -f package.json pnpm-workspace.yaml turbo.json pnpm-lock.yaml .nvmrc
          # Copy fixture files to current directory
          cp -r __fixtures__/biome-auto/* .

      - name: Setup with Biome auto-detect
        id: setup
        uses: ./.github/actions/runtime

      - name: Verify Biome installation
        run: |
          echo "## 🧬 Biome Auto-detect Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Biome Enabled: \`${{ steps.setup.outputs.biome-enabled }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Biome Version: \`${{ steps.setup.outputs.biome-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Biome Config: \`${{ steps.setup.outputs.biome-config-file }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Verify Biome is installed
          biome --version

          # Verify outputs
          if [ "${{ steps.setup.outputs.biome-enabled }}" != "true" ]; then
            echo "❌ Expected biome-enabled to be true" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          if [ "${{ steps.setup.outputs.biome-version }}" != "2.3.6" ]; then
            echo "❌ Expected version 2.3.6 from $schema" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          if [ "${{ steps.setup.outputs.biome-config-file }}" != "biome.jsonc" ]; then
            echo "❌ Expected biome.jsonc config file" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          # Run Biome check
          biome check test.js

          echo "✅ Biome auto-detection successful" >> $GITHUB_STEP_SUMMARY

  test-biome-explicit:
    name: Biome - Explicit version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test environment
        run: |
          # Remove repository config files
          rm -f package.json pnpm-workspace.yaml turbo.json pnpm-lock.yaml .nvmrc
          # Copy fixture files to current directory
          cp -r __fixtures__/node-minimal/* .
          echo 'const y = 2;' > test.js

      - name: Setup with explicit Biome version
        id: setup
        uses: ./.github/actions/runtime
        with:
          biome-version: "2.3.6"

      - name: Verify explicit Biome version
        run: |
          echo "## 🎯 Biome Explicit Version Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Biome Enabled: \`${{ steps.setup.outputs.biome-enabled }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Biome Version: \`${{ steps.setup.outputs.biome-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Verify Biome is installed
          INSTALLED_VERSION=$(biome --version | awk '{print $2}')
          echo "Installed: $INSTALLED_VERSION"

          if [ "${{ steps.setup.outputs.biome-version }}" != "2.3.6" ]; then
            echo "❌ Expected explicit version 2.3.6" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          biome check test.js
          echo "✅ Explicit Biome version successful" >> $GITHUB_STEP_SUMMARY

  test-turbo-detection:
    name: Turbo - Monorepo detection
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test environment
        run: |
          # Remove repository config files
          rm -f package.json pnpm-workspace.yaml biome.jsonc pnpm-lock.yaml .nvmrc
          # Copy fixture files to current directory
          cp -r __fixtures__/turbo-monorepo/* .

      - name: Setup with Turbo detection
        id: setup
        uses: ./.github/actions/runtime

      - name: Verify Turbo detection
        run: |
          echo "## ⚡ Turbo Detection Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Turbo Enabled: \`${{ steps.setup.outputs.turbo-enabled }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Turbo Config: \`${{ steps.setup.outputs.turbo-config-file }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Verify outputs
          if [ "${{ steps.setup.outputs.turbo-enabled }}" != "true" ]; then
            echo "❌ Expected turbo-enabled to be true" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          if [ "${{ steps.setup.outputs.turbo-config-file }}" != "turbo.json" ]; then
            echo "❌ Expected turbo.json config file" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          echo "✅ Turbo detection successful" >> $GITHUB_STEP_SUMMARY

  test-cache-effectiveness:
    name: Cache - First run vs cached run
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test environment
        run: |
          # Remove repository config files
          rm -f package.json pnpm-workspace.yaml biome.jsonc turbo.json pnpm-lock.yaml .nvmrc
          # Copy fixture files to current directory
          cp -r __fixtures__/cache-test/* .

      - name: First run (cache miss)
        id: setup1
        uses: ./.github/actions/runtime

      - name: Verify first run
        run: |
          echo "## 💾 Cache First Run Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Cache Hit: \`${{ steps.setup1.outputs.cache-hit }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # First run should not have cache hit
          if [ "${{ steps.setup1.outputs.cache-hit }}" == "true" ]; then
            echo "❌ First run should not have cache hit" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          # Verify dependencies installed
          if [ ! -d "node_modules/lodash" ]; then
            echo "❌ Dependencies not installed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          echo "✅ First run successful (no cache)" >> $GITHUB_STEP_SUMMARY

      - name: Clear node_modules for second run
        run: rm -rf node_modules

      - name: Second run (cache hit expected)
        id: setup2
        uses: ./.github/actions/runtime

      - name: Verify second run
        run: |
          echo "## 💾 Cache Second Run Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Cache Hit: \`${{ steps.setup2.outputs.cache-hit }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Second run should have cache hit or partial
          if [ "${{ steps.setup2.outputs.cache-hit }}" == "false" ]; then
            echo "❌ Second run should have cache hit or partial" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          # Verify dependencies still work
          if [ ! -d "node_modules/lodash" ]; then
            echo "❌ Dependencies not restored from cache" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          echo "✅ Cache restoration successful" >> $GITHUB_STEP_SUMMARY

  test-skip-deps:
    name: Skip dependency installation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup test environment
        run: |
          # Remove repository config files
          rm -f package.json pnpm-workspace.yaml biome.jsonc turbo.json pnpm-lock.yaml .nvmrc
          # Copy fixture files to current directory
          cp -r __fixtures__/cache-test/* .

      - name: Setup with install-deps disabled
        id: setup
        uses: ./.github/actions/runtime
        with:
          install-deps: false

      - name: Verify dependencies not installed
        run: |
          echo "## 🚫 Skip Dependencies Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- Runtime: \`${{ steps.setup.outputs.runtime }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Node Version: \`${{ steps.setup.outputs.node-version }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Verify Node.js is installed
          node --version
          npm --version

          # Verify node_modules was NOT created
          if [ -d "node_modules" ]; then
            echo "❌ node_modules should not exist when install-deps is false" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi

          echo "✅ Dependencies correctly skipped" >> $GITHUB_STEP_SUMMARY

  summary:
    name: Features Tests Summary
    runs-on: ubuntu-latest
    needs:
      [
        test-biome-auto-detect,
        test-biome-explicit,
        test-turbo-detection,
        test-cache-effectiveness,
        test-skip-deps,
      ]
    if: always()
    steps:
      - name: Generate summary
        run: |
          echo "## 🧪 Features Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Biome Auto-detect | ${{ needs.test-biome-auto-detect.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Biome Explicit | ${{ needs.test-biome-explicit.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Turbo Detection | ${{ needs.test-turbo-detection.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Cache Effectiveness | ${{ needs.test-cache-effectiveness.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Skip Dependencies | ${{ needs.test-skip-deps.result == 'success' && '✅ Pass' || '❌ Fail' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Check if all tests passed
          if [ "${{ needs.test-biome-auto-detect.result }}" == "success" ] && \
             [ "${{ needs.test-biome-explicit.result }}" == "success" ] && \
             [ "${{ needs.test-turbo-detection.result }}" == "success" ] && \
             [ "${{ needs.test-cache-effectiveness.result }}" == "success" ] && \
             [ "${{ needs.test-skip-deps.result }}" == "success" ]; then
            echo "### ✅ All feature tests passed!" >> $GITHUB_STEP_SUMMARY
            exit 0
          else
            echo "### ❌ Some tests failed" >> $GITHUB_STEP_SUMMARY
            exit 1
          fi
